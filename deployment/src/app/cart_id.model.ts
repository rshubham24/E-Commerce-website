export interface CartIdModel {
  title: string;
  price: number;
  category: string;
  imageUrl: string;
  retailerId: string;
  shopName: string;
  productId: string;
  customerId: string;
  quantity: number;
  id: string;
}

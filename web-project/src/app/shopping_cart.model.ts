export interface CartModel {
  title: string;
  price: number;
  category: string;
  imageUrl: string;
  retailerId: string;
  shopName: string;
  productId: string;
  customerId: string;
  quantity: number;
}
